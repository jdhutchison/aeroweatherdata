package weatherdata;

/**
 * The DataSources enum defines a key for each data source to tie various properties together
 */
public enum DataSource {
    AIRCRAFTREPORT,
    AIRSIGMET,
    METAR,
    STATION,
    TAF;
}
